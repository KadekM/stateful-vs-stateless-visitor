stateful-vs-stateless-visitor
=============================

Experiment to have a visitor which does not mutate any local state

Sadly we decide in Accept what to visit instead of in visitor's implementation, which is meh.
